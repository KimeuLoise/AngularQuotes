import { DateTransformsPipe } from './date-transforms.pipe';

describe('DateTransformsPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTransformsPipe();
    expect(pipe).toBeTruthy();
  });
});
